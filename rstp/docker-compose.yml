version: "3.9"

services:
  python_app:
    build: .
    container_name: python_app
    volumes:
      - .:/app
    # ports:
    #   - "8000:8000"
    # environment:
    #   - ENV=production
    restart: unless-stopped
