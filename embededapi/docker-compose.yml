# version: '3.8'

services:
  embededapi:
    build: ./
    container_name: embededapi
    ports:
      - "8000:8000"
    # networks:
    #   - api-cluster
    volumes:
      - ./.deepface/weights:/root/.deepface/weights

# networks:
#   api-cluster: